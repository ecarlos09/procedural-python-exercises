[[source]]
url = "https://pypi.org/simple"
verify_ssl = true
name = "pypi"

[packages]
pytest = "*"

[dev-packages]
pytest = "*"
pytest-cov = "*"

[requires]
python_version = "3.9"

[scripts]
app = "python app.py"
test = "pytest ."
coverage = "pytest --cov-report term-missing --cov=."
